/target
*.gguf
*.bin
models/*.gguf
models/*.bin
.env
.omc/
.omx/
.hermes-*
.zipcode-remote/

# Bench results are machine-generated (session JSON, cargo target/, model
# outputs). Keep the harness scripts in git, not the artifacts.
/benches/results/
